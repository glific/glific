query search($messageOpts: Opts!, $contactOpts: Opts!, $filter: SearchFilter!) {
  search(messageOpts: $messageOpts, contactOpts: $contactOpts, filter: $filter) {
    contact {
      id
      name
    }
    messages {
      id
      body
    }
  }
}
