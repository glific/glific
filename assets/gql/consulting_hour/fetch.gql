query fetchConsultingHours($filter: FetchConsultingHours) {
  fetchConsultingHours(filter: $filter) {
    id
    participants
    organizationName
    staff
    content
    when
    duration
    isBillable
  }
}
