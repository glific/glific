query contactsFields($filter: ContactsFieldFilter, $opts: Opts) {
  contactsFields(filter: $filter, opts: $opts) {
    id
    name
    shortcode
    value_type
    scope
    inserted_at
    updated_at
  }
}
