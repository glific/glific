mutation askme_bot($input: AskGlificInput!) {
    askGlific(input: $input) {
        answer
        conversationId
        conversationName
        messageId
        errors {
            message
        }
    }
}
