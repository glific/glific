query ask_glific_bot_conversations($limit: Int, $lastId: String) {
  askGlificBotConversations(limit: $limit, lastId: $lastId) {
    conversations {
      id
      name
      status
      createdAt
      updatedAt
    }
    hasMore
    limit
  }
}
