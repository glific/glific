query search($term: String!, $shouldSave: Boolean!, $searchLabel: String!) {
  search(term: $term, saveSearch: $shouldSave, searchLabel: $searchLabel) {
    contact {
      id
      name
      phone
    }
    messages {
      id
      body
      flow
      type
    }
  }
}
