import Config

config :glific,
  provider_url: "", # for gupshup this will be like: https://api.gupshup.io/sm/api/v1"
  # provider_url: "http://glific.test:4000/", # this is our local inbuilt proxy url which redirect all messages back with a PROXY prefix
  provider_key: "" # the gupshup API key found on your gupshup whatsapp dashboard
